import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('GreenInvest app smoke test', (WidgetTester tester) async {
    // Placeholder test — full widget tests require Hive/Riverpod setup.
    expect(true, isTrue);
  });
}
